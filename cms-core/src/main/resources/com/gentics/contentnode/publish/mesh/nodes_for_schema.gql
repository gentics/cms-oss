{
  schema(name: "%s") {
    nodes(perPage: %d, page: %d) {
      hasNextPage
      elements {
        uuid
        parent {
          uuid
        }
        languages {
          language
        }
      }
    }
  }
}
