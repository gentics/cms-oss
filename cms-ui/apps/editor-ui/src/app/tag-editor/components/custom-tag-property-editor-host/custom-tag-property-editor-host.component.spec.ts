
/**
 * TODO: Implement tests after feature release in Dec 2018.
 */
describe('CustomTagPropertyEditorHostComponent', () => {

    describe('initialization', () => {

        it('loads an external editor in an IFrame and initializes it properly (including calling registerOnSizeChange())', () => {});

        it('forwards TagPropertyEditor method calls that were made before the IFrame had completed loading', () => {});

    });

    describe('operation', () => {

        it('onChangeFn works correctly', () => {});

        it('writeChangedValues() is forwarded correctly', () => {});

        it('onSizeChangeFn updates the size of the IFrame', () => {});

        it('variableContext works', () => {});

        it('context.validator works', () => {});

    });

});
