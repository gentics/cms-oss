
/**
 * TODO: Implement tests after feature release in Dec 2018.
 */
describe('OverviewTagPropertyEditor', () => {

    describe('initialization', () => {

        it('initializes properly for unset Overview, all allowedListTypes, and all allowedSelectTypes', () => {});

        it('initializes properly for unset Overview, one allowedListType, and one allowedSelectType', () => {});

        it('initializes properly for set Overview, two allowedListTypes, and two allowedSelectTypes', () => {});

        it('initializes properly for hideSortingOptions = true', () => {});

    });

    describe('user input handling', () => {

        it('changing selectType works, clears the selected items, and triggers onChangeFn', () => {});

        it('changing listType works and triggers onChangeFn', () => {});

        it('changing selectType Auto hides clears and hides the selected items selector and triggers onChangeFn', () => {});

        it('adding selected items works and triggers onChangeFn', () => {});

        it('reordering selected items works and triggers onChangeFn', () => {});

        it('deleting selected items works and triggers onChangeFn', () => {});

        it('adding selected items with stickyChannel = true sets selectedNodeItemIds correctly and triggers onChangeFn', () => {});

    });

    describe('writeChangedValues()', () => {

        it('handles writeChangedValues() correctly', () => {});

    });

});
