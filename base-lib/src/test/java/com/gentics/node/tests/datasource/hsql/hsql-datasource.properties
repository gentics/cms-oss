# handle properties for a hsql handle

# this handle is used in the com.gentics.tests.datasource.HSQLDatasourceTest

# The type of the connection (jdbc or jndi)
type		= jdbc
driverClass	= org.hsqldb.jdbcDriver
url			= jdbc:hsqldb:file:${dbrootpath}/sample
username	= sa
shutDownCommand = SHUTDOWN
