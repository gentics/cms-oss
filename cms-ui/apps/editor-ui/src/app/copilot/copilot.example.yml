# Content Copilot configuration — drop this file at:
#
#     {ui-conf}/config/copilot.yml
#
# (the same {ui-conf} folder that already serves `ui-overrides.json`).
#
# When the file is missing or `enabled: false`, the toolbar button stays
# hidden and the sidebar never mounts. Actions are intentionally empty in
# this initial UI scaffolding — populate the list to make the entries
# appear in the sidebar; no UI rebuild required.

enabled: true

actions:
    # - id: summarize
    #   label: copilot.action_summarize_label
    #   icon: summarize
    #   description: copilot.action_summarize_description
    #   prompt: |
    #     Fasse den aktuellen Seiteninhalt in maximal drei Sätzen zusammen.
