/**
 * UNIT TESTS DISABLED
 */

import { TestBed } from '@angular/core/testing';
import { EntityExistsValidator } from './entity-exists-validator.service';

xdescribe('EntityExistsValidator', () => {
    beforeEach(() => TestBed.configureTestingModule({}));

});
