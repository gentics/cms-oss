package com.gentics.contentnode.rest.model;

/**
 * Type of localization (whole Page or only root content tags).
 */
public enum LocalizationType {
	/** Localize the entire page. */
	FULL,
	/** Localize only root content tags. */
	PARTIAL
}
