Editor User Interface: The character picker can now display icons for symbols, which are invisible (shy and non-breaking space for example). The definitions of all symbols have been revised and are now grouped together by topic.
